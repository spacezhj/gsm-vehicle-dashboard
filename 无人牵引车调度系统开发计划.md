# 无人牵引车调度系统开发计划

## 项目概述

本项目旨在开发一套无人牵引车调度系统的前端展示平台，实现单车选点发车、云端派单任务展示、多车调度可视化等功能。所有数据暂时使用静态数据进行模拟，不涉及后端服务开发。

---

## 技术栈

### 前端技术栈
- **核心框架**: Vue 3.3+ (Composition API)
- **构建工具**: Vite 4.x
- **状态管理**: Pinia
- **路由管理**: Vue Router 4
- **UI 组件库**: Element Plus
- **开发语言**: TypeScript 5.x
- **实时通信**: WebSocket (模拟)
- **数据可视化**: ECharts 5.x
- **地图组件**: Leaflet + vue-leaflet
- **代码规范**: ESLint + Prettier
- **版本控制**: Git

### 开发工具
- **包管理器**: pnpm / npm
- **调试工具**: Vue DevTools
- **API 模拟**: Mock.js / Vite Mock 插件
- **样式方案**: SCSS + TailwindCSS (可选)

---

## 项目时间节点安排

| 项目阶段 / 时间节点                | 阶段目标                                                  | 主要任务                                                                        | 备注              |
| -------------------------- | ----------------------------------------------------- | --------------------------------------------------------------------------- | --------------- |
| **第一阶段: 单车版选点发车**<br>下周五 (04/24) 前 | 1. 支持在车载屏上直接设置起点、终点并发车<br>2. 使用集世迈园区的静态站点坐标<br>3. 完成页面交互和状态模拟 | 1. 项目初始化与环境配置<br>2. 站点选择页面开发<br>3. 发车控制面板开发<br>4. 车辆状态模拟展示<br>5. 静态坐标数据整理 | 下周二进行本地集成测试 |
| **第二阶段: 单车云端派单展示**<br>04/30 前      | 1. 展示云端下发任务的模拟数据<br>2. 实现移动端下单页面（静态）<br>3. 采集和设计吉利园区的静态站点坐标     | 1. 任务列表页面开发<br>2. 订单详情展示<br>3. 移动端适配页面开发<br>4. 吉利园区站点数据采集与整理<br>5. 派单流程模拟 | 静态数据模拟完整流程 |
| **第三阶段: 多车调度可视化**<br>05/05 前      | 1. 展示多车调度的模拟场景<br>2. 实现任务分配、路径协同、防撞及充电策略的可视化展示<br>3. 优化整体用户体验 | 1. 多车监控大屏开发<br>2. 路径规划可视化<br>3. 冲突检测动画演示<br>4. 充电站管理页面<br>5. 性能优化与界面美化 | 重点在于可视化效果 |

---

## 功能模块划分

### 1. 车载屏控制模块
- **站点选择**: 下拉框/地图选点方式选择起点和终点
- **发车控制**: 启动、暂停、急停按钮
- **状态显示**: 当前位置、速度、电量、任务状态
- **告警提示**: 异常状态弹窗提醒

### 2. 任务管理模块
- **任务列表**: 展示所有任务（待执行、执行中、已完成）
- **任务详情**: 任务 ID、起点、终点、创建时间、状态等
- **任务下发**: 模拟云端派单操作
- **历史记录**: 历史任务查询与导出

### 3. 车辆监控模块
- **车辆列表**: 所有车辆基本信息展示
- **实时位置**: 地图上实时显示车辆位置（模拟轨迹）
- **状态监控**: 电量、速度、载重、故障码等
- **视频预览**: 车载摄像头画面占位符

### 4. 调度可视化模块
- **全局视图**: 园区地图全览，多车同时展示
- **路径规划**: 显示每辆车的规划路径和实际轨迹
- **冲突预警**: 潜在碰撞点的视觉提示
- **充电站管理**: 充电桩状态、排队情况展示

### 5. 数据统计模块
- **运营报表**: 日/周/月任务统计
- **效率分析**: 平均任务时长、车辆利用率
- **图表展示**: ECharts 柱状图、折线图、饼图

---

## 开发环境配置

### 1. 项目初始化
```bash
# 创建项目
pnpm create vite agv-scheduling-system --template vue-ts

# 安装依赖
cd agv-scheduling-system
pnpm install

# 安装核心依赖
pnpm install vue-router pinia element-plus @element-plus/icons-vue
pnpm install echarts leaflet @vue-leaflet/vue-leaflet
pnpm install axios mockjs
pnpm install -D sass tailwindcss autoprefixer

# 初始化 TailwindCSS (可选)
npx tailwindcss init -p
```

### 2. 目录结构
```
agv-scheduling-system/
├── public/
│   └── mock-data/          # 静态模拟数据
├── src/
│   ├── assets/             # 静态资源
│   ├── components/         # 公共组件
│   │   ├── common/         # 通用组件
│   │   ├── map/            # 地图相关组件
│   │   └── chart/          # 图表组件
│   ├── views/              # 页面视图
│   │   ├── vehicle/        # 车载屏页面
│   │   ├── task/           # 任务管理页面
│   │   ├── monitor/        # 监控页面
│   │   └── dashboard/      # 数据大屏
│   ├── stores/             # Pinia 状态管理
│   ├── router/             # 路由配置
│   ├── utils/              # 工具函数
│   ├── mock/               # Mock 数据定义
│   ├── types/              # TypeScript 类型定义
│   ├── styles/             # 全局样式
│   ├── App.vue
│   └── main.ts
├── package.json
├── vite.config.ts
├── tsconfig.json
└── README.md
```

### 3. 开发服务器启动
```bash
pnpm dev
```

---

## 静态数据设计

### 1. 站点数据 (sites.json)
```json
{
  "sites": [
    {
      "id": "site_001",
      "name": "集世迈 - 装卸区 A",
      "code": "JSM-A",
      "coordinates": { "lat": 30.1234, "lng": 120.5678 },
      "type": "loading"
    },
    {
      "id": "site_002",
      "name": "集世迈 - 仓储区 B",
      "code": "JSM-B",
      "coordinates": { "lat": 30.1244, "lng": 120.5688 },
      "type": "storage"
    }
  ]
}
```

### 2. 车辆数据 (vehicles.json)
```json
{
  "vehicles": [
    {
      "id": "agv_001",
      "name": "牵引车 01 号",
      "status": "idle",
      "battery": 85,
      "speed": 0,
      "position": { "lat": 30.1234, "lng": 120.5678 },
      "currentTask": null
    }
  ]
}
```

### 3. 任务数据 (tasks.json)
```json
{
  "tasks": [
    {
      "id": "task_001",
      "vehicleId": "agv_001",
      "startSite": "site_001",
      "endSite": "site_002",
      "status": "completed",
      "createdAt": "2025-04-20T10:00:00Z",
      "completedAt": "2025-04-20T10:15:00Z"
    }
  ]
}
```

---

## 通信协议模拟设计

### WebSocket 模拟服务
由于不使用真实后端，通过 Mock 方式模拟 WebSocket 推送：

```typescript
// mock/websocket-mock.ts
export class MockWebSocket {
  private listeners: Map<string, Function[]> = new Map();

  // 模拟服务器推送车辆位置更新
  simulateVehiclePositionUpdate() {
    setInterval(() => {
      const data = {
        type: 'VEHICLE_POSITION',
        payload: {
          vehicleId: 'agv_001',
          position: { lat: 30.1234 + Math.random() * 0.001, lng: 120.5678 + Math.random() * 0.001 },
          speed: Math.random() * 2,
          battery: Math.max(0, 85 - Math.random())
        }
      };
      this.emit('message', data);
    }, 1000);
  }

  // 模拟任务下发
  simulateTaskDispatch() {
    setTimeout(() => {
      const data = {
        type: 'NEW_TASK',
        payload: {
          taskId: `task_${Date.now()}`,
          vehicleId: 'agv_001',
          startSite: 'site_001',
          endSite: 'site_002'
        }
      };
      this.emit('message', data);
    }, 5000);
  }

  on(event: string, callback: Function) {
    if (!this.listeners.has(event)) {
      this.listeners.set(event, []);
    }
    this.listeners.get(event)!.push(callback);
  }

  emit(event: string, data: any) {
    const callbacks = this.listeners.get(event) || [];
    callbacks.forEach(cb => cb(data));
  }
}
```

---

## 页面路由设计

```typescript
// router/index.ts
const routes = [
  {
    path: '/',
    redirect: '/dashboard'
  },
  {
    path: '/dashboard',
    name: 'Dashboard',
    component: () => import('@/views/dashboard/Index.vue'),
    meta: { title: '数据大屏' }
  },
  {
    path: '/vehicle',
    name: 'VehicleControl',
    component: () => import('@/views/vehicle/ControlPanel.vue'),
    meta: { title: '车载控制' }
  },
  {
    path: '/task',
    name: 'TaskManagement',
    component: () => import('@/views/task/List.vue'),
    meta: { title: '任务管理' }
  },
  {
    path: '/monitor',
    name: 'VehicleMonitor',
    component: () => import('@/views/monitor/Index.vue'),
    meta: { title: '车辆监控' }
  },
  {
    path: '/mobile-order',
    name: 'MobileOrder',
    component: () => import('@/views/task/MobileOrder.vue'),
    meta: { title: '移动端下单', hidden: true }
  }
];
```

---

## 测试计划

### 1. 单元测试
- 组件功能测试 (Vitest + Vue Test Utils)
- 工具函数测试
- 状态管理测试

### 2. 集成测试
- 页面跳转流程测试
- 表单提交与验证
- 模拟数据加载与展示

### 3. 手动测试
- 各功能模块完整性检查
- 界面响应式适配测试
- 浏览器兼容性测试 (Chrome/Firefox/Edge)

---

## 风险管理

| 风险项 | 风险等级 | 应对措施 |
|--------|----------|----------|
| 静态数据与真实场景差异大 | 中 | 预留数据接口，便于后期接入真实后端 |
| 地图组件性能问题 | 中 | 优化渲染逻辑，限制同时展示的车辆数量 |
| 时间进度紧张 | 高 | 优先完成核心功能，非关键功能可延后 |
| 多车调度算法复杂度高 | 高 | 前期仅做可视化展示，算法逻辑简化处理 |

---

## 交付物清单

### 第一阶段交付物
- [ ] 项目基础框架搭建完成
- [ ] 车载屏控制页面（起点/终点选择、发车按钮）
- [ ] 集世迈园区静态站点数据
- [ ] 车辆状态模拟展示
- [ ] 基础文档（README、部署说明）

### 第二阶段交付物
- [ ] 任务管理页面（列表、详情）
- [ ] 移动端下单页面（H5 适配）
- [ ] 吉利园区静态站点数据
- [ ] 派单流程模拟演示

### 第三阶段交付物
- [ ] 多车监控大屏
- [ ] 路径规划可视化
- [ ] 充电站管理页面
- [ ] 数据统计图表
- [ ] 完整的项目文档和使用手册

---

## 项目目录结构（最终版）

```
agv-scheduling-system/
├── public/
│   └── mock-data/
│       ├── sites-jsm.json       # 集世迈站点
│       ├── sites-jl.json        # 吉利站点
│       ├── vehicles.json        # 车辆数据
│       └── tasks.json           # 任务数据
├── src/
│   ├── assets/
│   │   ├── images/
│   │   └── icons/
│   ├── components/
│   │   ├── common/
│   │   │   ├── Header.vue
│   │   │   ├── Sidebar.vue
│   │   │   └── StatusTag.vue
│   │   ├── map/
│   │   │   ├── SiteMap.vue
│   │   │   ├── VehicleMarker.vue
│   │   │   └── PathLine.vue
│   │   └── chart/
│   │       ├── TaskChart.vue
│   │       └── BatteryChart.vue
│   ├── views/
│   │   ├── vehicle/
│   │   │   └── ControlPanel.vue
│   │   ├── task/
│   │   │   ├── List.vue
│   │   │   ├── Detail.vue
│   │   │   └── MobileOrder.vue
│   │   ├── monitor/
│   │   │   └── Index.vue
│   │   └── dashboard/
│   │       └── Index.vue
│   ├── stores/
│   │   ├── vehicle.ts
│   │   ├── task.ts
│   │   └── site.ts
│   ├── router/
│   │   └── index.ts
│   ├── utils/
│   │   ├── mock-websocket.ts
│   │   ├── formatters.ts
│   │   └── validators.ts
│   ├── mock/
│   │   ├── sites.ts
│   │   ├── vehicles.ts
│   │   └── tasks.ts
│   ├── types/
│   │   └── index.ts
│   ├── styles/
│   │   ├── variables.scss
│   │   └── global.scss
│   ├── App.vue
│   └── main.ts
├── package.json
├── vite.config.ts
├── tsconfig.json
├── .eslintrc.cjs
├── .prettierrc
├── index.html
└── README.md
```

---

## 后续扩展建议

1. **后端接入**: 预留 API 接口，便于后期接入真实后端服务
2. **真实 WebSocket**: 替换 Mock WebSocket 为真实连接
3. **权限管理**: 添加用户登录和角色权限控制
4. **国际化**: 支持多语言切换
5. **主题定制**: 支持深色/浅色主题切换
6. **PWA 支持**: 添加离线访问能力

---

*文档版本：v1.0*  
*最后更新：2025 年 4 月*  
*编制人：开发团队*
